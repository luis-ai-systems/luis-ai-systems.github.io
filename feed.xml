<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://luis-ai-systems.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://luis-ai-systems.github.io/" rel="alternate" type="text/html" /><updated>2026-03-24T13:51:55+08:00</updated><id>https://luis-ai-systems.github.io/feed.xml</id><title type="html">Luis Li</title><subtitle>技术，生活</subtitle><author><name>Luis Li</name></author><entry><title type="html">在docker-desktop中部署gitea</title><link href="https://luis-ai-systems.github.io/2025/04/01/docker-gitea/" rel="alternate" type="text/html" title="在docker-desktop中部署gitea" /><published>2025-04-01T00:00:00+08:00</published><updated>2025-04-01T00:00:00+08:00</updated><id>https://luis-ai-systems.github.io/2025/04/01/docker-gitea</id><content type="html" xml:base="https://luis-ai-systems.github.io/2025/04/01/docker-gitea/"><![CDATA[<h1 id="在docker-desktop中部署gitea">在docker-desktop中部署gitea</h1>

<h2 id="环境准备">环境准备</h2>]]></content><author><name>Luis Li</name></author><category term="gitea,docker" /><summary type="html"><![CDATA[gitea用docker部署]]></summary></entry><entry><title type="html">在docker-desktop中启动kubernets</title><link href="https://luis-ai-systems.github.io/2025/03/30/docker-kubernets/" rel="alternate" type="text/html" title="在docker-desktop中启动kubernets" /><published>2025-03-30T00:00:00+08:00</published><updated>2025-03-30T00:00:00+08:00</updated><id>https://luis-ai-systems.github.io/2025/03/30/docker-kubernets</id><content type="html" xml:base="https://luis-ai-systems.github.io/2025/03/30/docker-kubernets/"><![CDATA[<h1 id="在docker-desktop中启动kubernets">在docker-desktop中启动kubernets</h1>
<p><img src="/images/posts/docker/1100.png" alt="点击开关" />
<img src="/images/posts/docker/1200.png" alt="可以调节节点数量" />
这个过程会去下载镜像</p>

<h1 id="在容器界面">在容器界面</h1>
<p><img src="/images/posts/docker/1300.png" alt="可以看到cluster的容器" /></p>

<h2 id="创建dashboard">创建dashboard</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PS C:<span class="se">\U</span>sers<span class="se">\L</span>enovo&gt; kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/dashboard/v2.7.0/aio/deploy/recommended.yaml
namespace/kubernetes-dashboard created
serviceaccount/kubernetes-dashboard created
service/kubernetes-dashboard created
secret/kubernetes-dashboard-certs created
secret/kubernetes-dashboard-csrf created
secret/kubernetes-dashboard-key-holder created
configmap/kubernetes-dashboard-settings created
role.rbac.authorization.k8s.io/kubernetes-dashboard created
clusterrole.rbac.authorization.k8s.io/kubernetes-dashboard created
rolebinding.rbac.authorization.k8s.io/kubernetes-dashboard created
clusterrolebinding.rbac.authorization.k8s.io/kubernetes-dashboard created
deployment.apps/kubernetes-dashboard created
service/dashboard-metrics-scraper created
deployment.apps/dashboard-metrics-scraper created
</code></pre></div></div>
<h4 id="通过代理方式进入">通过代理方式进入</h4>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl proxy <span class="nt">--port</span><span class="o">=</span>8001
</code></pre></div></div>

<pre><code class="language-输入url">http://localhost:8001/api/v1/namespaces/kubernetes-dashboard/services/https:kubernetes-dashboard:/proxy/#/login
</code></pre>
<p>进入<img src="\images/posts/docker/1400.png" alt="dashborad界面" /></p>

<p>检查 kubernetes-dashboard 命名空间下的 ServiceAccount 是否已经存在：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get serviceaccount -n kubernetes-dashboard
</code></pre></div></div>

<p>显示内容如下：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PS C:\Users\Lenovo&gt; kubectl get serviceaccount -n kubernetes-dashboard
NAME                   SECRETS   AGE
default                0         153m
kubernetes-dashboard   0         153m
</code></pre></div></div>
<p>然后获取service-token</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl -n kubernetes-dashboard create token kubernetes-dashboard
</code></pre></div></div>
<p>进入页面如下
<a href="images/posts/docker/1500.png">kuberbets界面</a></p>

<h4 id="但是上面进入的权限不够创建新的服务名">但是上面进入的权限不够创建新的服务名</h4>
<p>1️⃣ 创建 admin-user ServiceAccount</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create serviceaccount admin-user -n kubernetes-dashboard
</code></pre></div></div>
<p>2️⃣ 创建 ClusterRoleBinding，授予 admin-user cluster-admin 权限</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create clusterrolebinding admin-user \
  --clusterrole=cluster-admin \
  --serviceaccount=kubernetes-dashboard:admin-user
</code></pre></div></div>
<p>3️⃣ 获取 admin-user 登录的 Token</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl -n kubernetes-dashboard create token admin-user
</code></pre></div></div>

<p>asdsadsadsadsaduahd</p>]]></content><author><name>Luis Li</name></author><category term="kubernets" /><summary type="html"><![CDATA[开始学习kubernets]]></summary></entry><entry><title type="html">docker-compose部署gitea-CI/CD actions runner工作流</title><link href="https://luis-ai-systems.github.io/2025/03/24/excel+ai/" rel="alternate" type="text/html" title="docker-compose部署gitea-CI/CD actions runner工作流" /><published>2025-03-24T00:00:00+08:00</published><updated>2025-03-24T00:00:00+08:00</updated><id>https://luis-ai-systems.github.io/2025/03/24/excel+ai</id><content type="html" xml:base="https://luis-ai-systems.github.io/2025/03/24/excel+ai/"><![CDATA[<p>=COUNTIF($I:$I, G1)&gt;0和 =SUMPRODUCT(–(COUNTIF($I:$I, G1:G1000)&gt;0)) 给我详细介绍下
ChatGPT 说：
让我们详细拆解这两个 Excel 公式的工作原理，帮助你理解它们的作用和区别。</p>

<ol>
  <li>=COUNTIF($I:$I, G1)&gt;0
作用
判断 G1 单元格的值是否在 I 列中出现。</li>
</ol>

<p>如果 G1 在 I 列中至少出现 1 次，则返回 TRUE（即 1），否则返回 FALSE（即 0）。</p>

<p>这个公式通常用于 条件格式，用于 标记 出现在 I 列中的 G 列值。</p>

<p>拆解
COUNTIF($I:$I, G1)：统计 G1 在 I 列中出现的次数。</p>

<blockquote>
  <p>0：如果出现次数 大于 0，则返回 TRUE（即 1），否则返回 FALSE（即 0）。</p>
</blockquote>

<p>示例
假设 I 列的数据如下：</p>

<p>I 列
A
B
C
如果 G1 = B：</p>

<p>COUNTIF($I:$I, G1) = 1（因为 B 在 I 列中出现 1 次）</p>

<p>1 &gt; 0，结果返回 TRUE（即 1）。</p>

<p>如果 G1 = D：</p>

<p>COUNTIF($I:$I, G1) = 0（因为 D 不在 I 列中）</p>

<p>0 &gt; 0，结果返回 FALSE（即 0）。</p>

<ol>
  <li>=SUMPRODUCT(–(COUNTIF($I:$I, G1:G1000)&gt;0))
作用
计算 G1:G1000 这些单元格中，有多少个值出现在 I 列中。</li>
</ol>

<p>适用于统计符合 COUNTIF($I:$I, G1)&gt;0 条件的单元格总数。</p>

<p>拆解
COUNTIF($I:$I, G1:G1000)：</p>

<p>这个部分会 批量 计算 G1:G1000 这些单元格在 I 列中出现的次数，并返回一个数组。</p>

<p>例如：</p>

<p>excel
复制
编辑
{2, 0, 1, 3, 0}
代表：</p>

<p>G1 在 I 列中出现 2 次</p>

<p>G2 在 I 列中出现 0 次</p>

<p>G3 在 I 列中出现 1 次</p>

<p>G4 在 I 列中出现 3 次</p>

<p>G5 在 I 列中出现 0 次</p>

<p>COUNTIF($I:$I, G1:G1000)&gt;0：</p>

<p>把 出现次数 转换成 TRUE/FALSE（即 &gt;0 的是 TRUE，否则是 FALSE）：</p>

<p>excel
复制
编辑
{TRUE, FALSE, TRUE, TRUE, FALSE}
代表：</p>

<p>G1 在 I 列中 出现（TRUE）</p>

<p>G2 在 I 列中 没出现（FALSE）</p>

<p>G3 在 I 列中 出现（TRUE）</p>

<p>G4 在 I 列中 出现（TRUE）</p>

<p>G5 在 I 列中 没出现（FALSE）</p>

<p>–(…)：</p>

<p>把 TRUE/FALSE 变成 1/0，变成：</p>

<p>excel
复制
编辑
{1, 0, 1, 1, 0}
1 代表该值出现在 I 列中，0 代表该值 未出现在 I 列。</p>

<p>SUMPRODUCT(…)：</p>

<p>把所有 1 相加，计算 符合条件的单元格总数。</p>

<p>在这个例子中：</p>

<p>excel
复制
编辑
1 + 0 + 1 + 1 + 0 = 3
说明 G1:G1000 这些单元格中，有 3 个值出现在 I 列中。</p>

<p>总结
公式	作用	结果
=COUNTIF($I:$I, G1)&gt;0	检查 G1 是否在 I 列中存在	TRUE/FALSE 或 1/0
=SUMPRODUCT(–(COUNTIF($I:$I, G1:G1000)&gt;0))	统计 G1:G1000 区间内有多少个值出现在 I 列	数字（符合条件的单元格数）
如果你要 在条件格式中 使用，则适合 COUNTIF($I:$I, G1)&gt;0，因为它是针对 单个单元格 的。
如果你要 统计有多少个单元格被染色，则 SUMPRODUCT(–(COUNTIF($I:$I, G1:G1000)&gt;0)) 是正确的选择。</p>]]></content><author><name>Luis Li</name></author><category term="gitea" /><summary type="html"><![CDATA[devops部署，包括docker-compose容器管理，actions runner调用工作流]]></summary></entry><entry><title type="html">docker-compose部署gitea-CI/CD actions runner工作流</title><link href="https://luis-ai-systems.github.io/2025/03/19/devops/" rel="alternate" type="text/html" title="docker-compose部署gitea-CI/CD actions runner工作流" /><published>2025-03-19T00:00:00+08:00</published><updated>2025-03-19T00:00:00+08:00</updated><id>https://luis-ai-systems.github.io/2025/03/19/devops</id><content type="html" xml:base="https://luis-ai-systems.github.io/2025/03/19/devops/"><![CDATA[<p>Gitea 的 <strong>Actions Runner</strong> 是 Gitea 官方提供的 CI/CD 运行器，它可以让你在 Gitea 内部直接执行 CI/CD 流程，类似于 GitHub Actions 或 GitLab CI/CD。</p>

<h3 id="gitea-actions-runner-是什么"><strong>Gitea Actions Runner 是什么？</strong></h3>
<p>它用于运行 Gitea Actions 定义的任务，比如：</p>
<ul>
  <li><strong>编译构建 Java 项目</strong>（使用 Maven/Gradle）</li>
  <li><strong>运行自动化测试</strong></li>
  <li><strong>打包 Docker 镜像</strong></li>
  <li><strong>部署应用到服务器/K8s</strong></li>
</ul>

<p>它的作用类似于 Drone、Jenkins，但它是 <strong>Gitea 官方内置的 CI/CD 解决方案</strong>，不需要额外的 CI/CD 工具。</p>

<hr />

<h3 id="如何使用-gitea-actions-runner"><strong>如何使用 Gitea Actions Runner</strong></h3>
<h4 id="1-启用-gitea-actions"><strong>1. 启用 Gitea Actions</strong></h4>
<p>你需要在 <code class="language-plaintext highlighter-rouge">app.ini</code> 配置文件中启用 Actions：</p>
<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[ACTIONS]</span>
<span class="py">ENABLED</span> <span class="p">=</span> <span class="s">true</span>
<span class="py">DEFAULT_ACTIONS_URL</span> <span class="p">=</span> <span class="s">https://gitea.com/gitea/act_runner/releases</span>
</code></pre></div></div>
<p>然后重启 Gitea。</p>

<h4 id="2-安装-gitea-actions-runner"><strong>2. 安装 Gitea Actions Runner</strong></h4>
<p>你需要在服务器上安装 <strong>Gitea Actions Runner</strong>，可以使用 Docker 运行：</p>
<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-d</span> <span class="nt">--name</span> gitea-runner <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">GITEA_INSTANCE_URL</span><span class="o">=</span>http://gitea:3000 <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">GITEA_RUNNER_REGISTRATION_TOKEN</span><span class="o">=</span>your_token <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">GITEA_RUNNER_NAME</span><span class="o">=</span>my-runner <span class="se">\</span>
  <span class="nt">-e</span> <span class="nv">GITEA_RUNNER_LABELS</span><span class="o">=</span>linux <span class="se">\</span>
  gitea/act_runner:latest
</code></pre></div></div>

<h4 id="3-在-gitea-项目中创建-giteaworkflowsyml"><strong>3. 在 Gitea 项目中创建 <code class="language-plaintext highlighter-rouge">.gitea/workflows.yml</code></strong></h4>
<p>类似于 GitHub Actions，你可以在仓库里创建 <code class="language-plaintext highlighter-rouge">.gitea/workflows.yml</code>，比如：</p>
<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Java CI/CD Pipeline</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">main</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout Code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up JDK</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-java@v3</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">java-version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">17'</span>
          <span class="na">distribution</span><span class="pi">:</span> <span class="s1">'</span><span class="s">temurin'</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build with Maven</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mvn clean package</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build Docker Image</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">docker build -t my-app .</span>
          <span class="s">docker tag my-app my-registry:5000/my-app:latest</span>
          <span class="s">docker push my-registry:5000/my-app:latest</span>
</code></pre></div></div>
<p><strong>这个 Workflow 作用：</strong></p>
<ol>
  <li>检出代码</li>
  <li>设置 Java 17 环境</li>
  <li>用 Maven 进行编译</li>
  <li>构建 Docker 镜像并推送到私有仓库</li>
</ol>

<hr />

<h3 id="gitea-actions-runner-vs-dronejenkins"><strong>Gitea Actions Runner vs. Drone/Jenkins</strong></h3>
<p>| 对比项          | Gitea Actions Runner | Drone CI | Jenkins |
|—————|—————-|———|———|
| <strong>易用性</strong>   | 内置 Gitea，简单 | 需要单独部署 | 复杂，需要配置 |
| <strong>扩展性</strong>   | 兼容 GitHub Actions 生态 | 轻量级，基于容器 | 插件丰富，功能强大 |
| <strong>部署难度</strong> | 直接跑容器即可 | 需要 Drone Server &amp; Runner | 需要 Master/Agent |
| <strong>适合场景</strong> | 适用于 Gitea 项目，轻量级 CI/CD | 适合容器化项目 | 适合大型企业级 CI/CD |</p>

<p><strong>适用场景：</strong></p>
<ul>
  <li>如果你已经在用 Gitea，推荐直接用 <strong>Gitea Actions</strong></li>
  <li>如果你的项目是容器化的，可以用 <strong>Drone</strong></li>
  <li>如果你需要高度自定义，可以用 <strong>Jenkins</strong></li>
</ul>

<hr />

<h3 id="总结"><strong>总结</strong></h3>
<p>如果你想让 Gitea 具备 CI/CD 能力，可以用 <strong>Gitea Actions Runner</strong> 直接跑自动化任务，而不需要额外部署 Drone 或 Jenkins。<br />
你可以在 <code class="language-plaintext highlighter-rouge">.gitea/workflows.yml</code> 里配置构建、测试、部署任务，类似 GitHub Actions，简单好用！ 🚀</p>]]></content><author><name>Luis Li</name></author><category term="gitea" /><summary type="html"><![CDATA[devops部署，包括docker-compose容器管理，actions runner调用工作流]]></summary></entry></feed>